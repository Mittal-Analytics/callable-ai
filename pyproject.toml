[project]
name = "mittal-ai"
version = "0.1.0"
description = "Mittal Analytics AI harness"
readme = "README.md"
authors = [{ name = "Mittal Analytics Pvt Ltd" }]
requires-python = ">=3.12"
dependencies = [
    "dj-evals>=0.6,<0.7",
    "django>=5.2,<6.2",
    "openai>=2.38,<3",
    "pydantic>=2.12,<3",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "mittal_ai"

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "pytest",
    "pytest-asyncio",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
